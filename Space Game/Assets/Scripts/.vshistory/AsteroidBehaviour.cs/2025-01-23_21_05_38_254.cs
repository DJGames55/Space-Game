using UnityEngine;

public class AsteroidBehaviour : MonoBehaviour
{
    // Function for when "bullet" collides with Asteroid
    // Asteroid takes damage untill destoyed
    // Each child object moves with momentem away from the centre for 2 seconds, then fades away

    // Asteroid has slow ambiant movement

    // If asteroids collide  a particle effect plays, and they bouce in opposite directions, health is lowered
}
